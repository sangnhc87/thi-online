rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Exam images: anyone authenticated can read, teachers can write
    match /exams/{examId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
